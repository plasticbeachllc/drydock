# Archived Docs

These files are retained for historical context while the repo converges on a single
canonical guide in the root `README.md`.

- `PLAN.md`: original repo bootstrap and migration plan
- `QOL_PLAN.md`: macOS runbook used to assemble the toolchain interactively
- `QOL_REFERENCE.md`: longer-form reference notes and package inventory
